#ifndef INPUT_H
#define INPUT_H

char* concat(const char *s1, const char *s2);
char* readInput();
char* readUsername();
char* readPassword();
char* readList();
char* readRetrN(int mail);
char* getMail();

#endif
